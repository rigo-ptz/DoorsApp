<?xml version="1.0" encoding="utf-8"?>
<layout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>
        <import type="com.jollypanda.petrsudoors.data.remote.response.ACTION"/>
        <variable name="view" type="com.jollypanda.petrsudoors.ui.result.SuccessActivity"/>
        <variable name="action" type="com.jollypanda.petrsudoors.data.remote.response.ACTION"/>
        <variable name="roomNumber" type="String"/>
        <variable name="roomFloor" type="String"/>
    </data>

    <android.support.constraint.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@color/colorPrimary">
        <ImageView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="@dimen/dp16"
            android:layout_marginStart="@dimen/dp16"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:srcCompat="@drawable/ic_back"
            android:onClick="@{() -> view.finish()}"/>

        <ImageView
            android:id="@+id/ivSuccess"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:srcCompat="@drawable/ic_success"/>

        <TextView
            android:id="@+id/tvRoomInfo"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="@dimen/dp32"
            app:layout_constraintTop_toBottomOf="@+id/ivSuccess"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            android:textColor="@android:color/white"
            android:text="@{@string/roomInfo(roomNumber, roomFloor)}"
            tools:text="Кабинет 251, Этаж 2"/>

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="@dimen/dp32"
            app:layout_constraintTop_toBottomOf="@+id/tvRoomInfo"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            android:textColor="@android:color/white"
            android:text="@{action == ACTION.RETURN_KEY ? @string/hint_key_returned : @string/hint_key_get}"
            tools:text="Ключ сдан"/>
    </android.support.constraint.ConstraintLayout>
</layout>